import { Button } from "@mui/material";

const HomePage = () => {
  return (
    <div>
      <Button variant="contained">Contained</Button>
      HomePage
    </div>
  );
};

export default HomePage;
